{
  "name": "resilient-email-service",
  "version": "1.0.0",
  "description": "A resilient email sending service with retries, fallback, rate limiting, idempotency, and status tracking.",
  "main": "dist/index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "test": "jest",
    "lint": "eslint . --ext .ts",
    "dev": "nodemon src/index.ts"
  },
  "author": "Aniket Kharade",
  "license": "MIT",
  "dependencies": {
    "ts-node": "^10.9.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "^20.6.0",
    "eslint": "^8.50.0",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "ts-jest": "^29.4.0",
    "typescript": "^5.8.3"
  }
}
